## Admin Interface Setup

1. **Model Registration**:
   - Registered `Book` in `bookshelf/admin.py` with custom `BookAdmin` class.
   - Configured list view, filters, and search.

2. **Usage**:
   - Access admin at `http://127.0.0.1:8000/admin/`.
   - Add/edit books via the interface.
